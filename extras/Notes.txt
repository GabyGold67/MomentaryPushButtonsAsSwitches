_ The begin() method can be reformatted as a class method template for each class in the library
_ For the latched subclasses the _validPressPending must be provided by different meanings, the rest would be the same...
